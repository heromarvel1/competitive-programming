#include <bits/stdc++.h>

using namespace std;

bool divsable(int a[],int n)
{
    for(int i=0;i<n;i++)
        if(a[i]%10 == 0)
        return true;
    return false;
}

int main()
{
    int A[]={9,8,7,6,4,2,1,30};
    int n = sizeof(A)/sizeof(A[0]);
    if(divsable(A,n)== true)
        cout<<"YES\n";
    else cout<<"NO\n";
    return 0;
}
